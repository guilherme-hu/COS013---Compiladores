function f(x) { 
  if( x % 2 == 0 ) { 
    let x = 1;

    function g() { 
      {
        return 3.1 + x++; 
        1; 
      }  
    }

    for( let i = 0; i < 5; i++ ) {     
      g();
    }
    return g() + x;
    let v;
  }
  else 
    return x; 
}

let a = f(4);
let b = f(3);